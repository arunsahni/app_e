/**
 * isAuthenticated
 * check if user is autheticated to application using token based approach
 * 
 * @module::  	Policy
 * @todo:: 		implement user authentication 
*/

module.exports = function(req, res, next) {
	return next();
};